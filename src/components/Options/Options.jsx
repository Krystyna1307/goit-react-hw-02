const Options = () => {
  return (
    <section>
      <button>Good</button>
      <button>Neutral</button>
      <button>Bad</button>
      <button>Reset</button>
    </section>
  );
};

export default Options;
